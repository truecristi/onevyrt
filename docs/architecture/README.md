# Architecture documentation

Per the root README: "Architecture documentation should be stored in
`docs/architecture/`."

Currently empty beyond this file - Phase 1 is a scaffold plus a single
auth/workspace vertical slice, thin enough that the ADRs in
`docs/decisions/` and the code itself are the accurate description of the
architecture. A dedicated architecture overview (system diagram, domain
map) is worth writing once there are enough bounded domains that the ADR
index alone stops being a sufficient map - expect that starting around
Phase 4-6.
