pnpm-lock.yaml
# Hand-authored by the user directly on GitHub - preserved exactly as written.
README.md
# Contains large blocks of raw (non-fenced) CSS/HTML as plain prose. Prettier's
# markdown formatter treats bare `*`/`_` in that text as emphasis markers and
# "normalizes" them, which silently corrupts literal `/* ... */` CSS comment
# delimiters (`/* text */` <-> `/_ text _/`) - this is what caused a real,
# hard-to-spot content bug the first time this file was formatted. README.md
# embeds this same document verbatim, which is why it's ignored above too.
docs/ONEVYRT_Deep_Product_Learning_AI_Implementation_Master_Spec.md
