# Parity tests

Empty for Phase 1 - there is no legacy system left to compare output
against (see `docs/parity/`'s source notes). This directory is reserved
for golden-result comparisons (§7's engine parity, §43's migration
reconciliation) once Phase 7/18 have something to compare against.
