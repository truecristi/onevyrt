# Shared tooling

Empty for Phase 1. Reserved for shared build/lint/codegen scripts once a
second package needs the same non-trivial tooling that today lives inline
in root config (`eslint.config.mjs`, `tsconfig.base.json`,
`.prettierrc.json`).
